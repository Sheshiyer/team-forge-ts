# Lessons

- Create `tasks/todo.md` and keep it updated before starting non-trivial project work in this repo.
- Review or create `tasks/lessons.md` at the start of the session when working in this repo.
- When a user asks to verify that a launcher "really runs" and not just that a server opens, prove the runtime path with a live functional invocation, not only a process check or browser-visible page.
- On macOS, verify bundled Tauri apps via `open TeamForge.app` or Finder/LaunchServices, not by directly executing `TeamForge.app/Contents/MacOS/...`; direct binary launch can crash inside AppKit registration even when the bundle itself is healthy.
- When the user wants org structure or department mapping, prefer an in-app editable workflow over a one-off manual mapping outside the product.
- When adding a new provider integration, match the existing Settings UX and explicitly name the exact credential type the user must paste; generic "token" fields are too ambiguous.
- When surfacing third-party API failures in the app, preserve structured details like the exact missing scope instead of collapsing everything into a generic error string.
- When the user is mapping people to roles and departments, default to a visual roster-and-drop workflow; dense dropdowns are the wrong interaction model for org assignment.
- When ignore/exclusion controls affect synced people, do not rely only on email addresses; provide a roster-based selector because Huly-sourced people may not have usable email fields.
- For Team data, a reduced number of live API calls is not enough; the page needs a persistent SQLite-backed cache and must render from cached data instead of blocking on Huly availability.
- When a page uses `useInvoke`, do not put the returned object directly into effect dependencies; stabilize the invoke surface first or the page can get stuck in a re-render / refresh loop that looks like endless loading.
- Huly workspaces do not always expose every class used by optional modules; Team-facing `find_all` calls for HR data must degrade gracefully on `404 INVALID CLASS NAME` instead of failing the whole snapshot refresh.
- When the Team page already shows leave and holiday data, users expect to manage local leave and yearly holiday tracking directly there; do not hide that workflow in a separate screen or depend on optional Huly HR modules for basic editing.
- When the user says a page is carrying too many responsibilities, fix the information architecture instead of squeezing more UI into it; split calendar and ops-heavy workflows into a dedicated route and keep the Team surface focused on people and org state.
- When org assignment depends on drag-and-drop, provide an explicit fallback control model; roster-based dropdown assignment is more reliable than pointer-dependent drag behavior for department and role mapping.
- For LCARS screens, avoid generic dashboard cards and boxed admin panels; build the interface from segmented rails, bands, strips, and console sections that feel native to the Star Trek shell already in the app.
- When the user asks for an overall design consistency overhaul, do not narrow the work to the page they mentioned first; audit and improve the shared shell, common controls, spacing system, and cross-page visual language together.
- When a user explicitly asks to update the README after shipping visible product or release changes, refresh the repo landing page in the same pass instead of treating docs as optional follow-up work.
- For small trusted teams, default cloud credential sync to enabled and require explicit opt-out; reducing setup friction for recurring sync workflows can be more important than adding extra toggle steps.
- When the user asks to print a credential and run a clean rebuild, do both in the same pass: return the live token value and produce a verified fresh app artifact, including fallback for updater-signing blockers.
- Do not equate “sync succeeded” with “page will render data”: verify each page’s query shape and ensure synced entities (for example, projects) are surfaced directly, not only through derived activity tables.
- When Tauri updater public keys are configured, tagged GitHub releases will fail to publish assets unless `TAURI_SIGNING_PRIVATE_KEY` is present or updater artifact generation is explicitly disabled in CI build args.
- When the embedded Tauri updater public key already matches the local TeamForge keypair, reuse that existing private key for CI secrets; do not generate a new updater key unless you are intentionally rotating the trust chain.
- When the user says TeamForge should be the main cross-sync system and wants the backend Cloudflare-based, do not make the desktop SQLite database the canonical source of truth; Cloudflare Worker + D1 must own project identity and sync state, with local SQLite only as cache/offline projection.
- When planning GitHub <-> Huly sync for TeamForge, do not assume issues are globally bidirectional; model issue ownership by domain, with GitHub owning engineering issues and Huly owning execution/admin issues.
- For TeamForge issue ownership, default to hybrid classification: rule-based issue domain detection with explicit durable manual override, not pure automation or pure manual tagging.
- For nested sub-tabs under a splat route (for example `/team/*`), do not rely on plain relative tab links; use stable absolute targets and include both `index` and `*` fallback redirects so deep or malformed URLs cannot blank the page.
- When upstream SDK packages are transitively broken on npm, do not stall feature delivery on dependency firefighting; pivot immediately to a REST transaction path that uses stable endpoint contracts already proven in this repo.
- When a page can load before Tauri command state is ready, never leave it with a one-shot fetch path; add retry + partial-failure fallback so startup races cannot freeze the UI into a false zero-state.
- When the user says they do not want hard-coded data, remove mock-derived trends and placeholder dashboard sections instead of dressing them up as “temporary” UI.
- When TeamForge/Cloudflare is the canonical project registry, do not make the execution Projects page depend on a control-plane fetch side effect; the execution path must hydrate or reuse the TeamForge bridge itself and surface upstream failure separately from empty local results.
- When a user ties a release push to milestone completion, do not preemptively bump version metadata; audit the milestone status first and keep release/version updates gated on that checkpoint.
- When a user says prior planning or wakeup context is stale because the subsystem already shipped, stop treating that thread as pending work; rebase immediately on the current shipped artifacts and inspect the actual files before proposing next steps.
- When doing Thoughtseed vault parity for TeamForge, do not stop at `60-client-ecosystem` project briefs; also inspect adjacent `50-team/*-kpi.md` records so employee performance context is not silently omitted.
- When vault parity says KPI imports succeeded locally but project creates still fail on the live Worker, treat it as a remote control-plane bug first; verify Worker/D1 schema and live exception context before changing the local parity script again.
- When the user narrows a follow-up slice, reflect that scope change immediately in the plan instead of carrying forward every previously suggested surface; in this case `Training` is deferred while `Onboarding` must stay split across both employee and client flows.
- When the user says the Team page should use Obsidian/vault data as the source of truth, do not preserve the old org-assignment editor out of inertia; switch the page to a straightforward vault-backed read model and remove ambiguous assignment controls.
- When a page is only heuristically inferring a domain object from unrelated project signals, do not expose it as a primary module in the shell; in this case the fake `Devices` registry had to be replaced with a true project-grouped `Issues` module.
- When the user defines TeamForge as the central founder-and-agent control plane, do not scope it narrowly to cloud integrations alone; model local vault access and local launcher actions as first-class Tauri features, while keeping Cloudflare/D1 as the shared source of truth and absolute local paths as per-machine settings only.
- When the user gives the concrete external runtime path for a local companion system, inspect and validate the existing launcher contract in that repo before inventing a new process model; for Thoughtseed Paperclip, the real instance path is the sibling repo and the TeamForge launcher must wrap its existing `babysitter.sh start` entrypoint rather than guess a generic script.
- When doing an app-wide UI cleanup, do not stop after polishing the largest screens; run a literal copy sweep across the remaining routes and normalize status, error, and empty-state language everywhere in the same pass.
