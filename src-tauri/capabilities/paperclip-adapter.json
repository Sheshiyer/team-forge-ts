{
  "identifier": "paperclip-adapter",
  "description": "Narrow capability for Paperclip adapter spawn via node script. Singleton constraint: one shared instance for all orgs. Rust ShellExt bypasses this ACL; this scopes JS-side calls only. TeamForge is operator surface only.",
  "windows": ["main"],
  "permissions": [
    {
      "identifier": "shell:allow-spawn",
      "allow": [
        {
          "name": "paperclip-adapter",
          "cmd": "node",
          "args": true,
          "sidecar": false
        }
      ]
    }
  ]
}
