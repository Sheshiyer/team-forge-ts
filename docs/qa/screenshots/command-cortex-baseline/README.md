# Command Cortex Baseline Screenshots

This folder is reserved for the baseline screenshots defined by Phase 1 task T-018.

Capture these before Phase 2 route integration if a browser/Tauri visual QA session is available:

- `overview-desktop.png` - classic overview at desktop width.
- `agents-desktop.png` - classic agents runtime page at desktop width.
- `overview-compact.png` - classic overview at compact desktop width.
- `command-palette.png` - current command palette open with `⌘K`.

Phase 1 completed the manifest and verification contract. Actual screenshots require a running app window or browser automation session.
