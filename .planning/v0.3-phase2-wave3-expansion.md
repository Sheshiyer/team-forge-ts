# Phase 2 Wave 3 Expansion — Ecosystem Bridge Surfaces & Shell Polish
# (Updated with singleton Paperclip constraint)
# Original Wave 3 goal: Make the four bridges visible and operable from the shell + core screens, using embeddings for relevance.
# Updated understanding: Paperclip = ONE shared singleton instance (fixed port + adapter) serving the entire Thoughtseed ecosystem (multiple orgs inside that one instance). TeamForge (C) is the operator surface talking to it via bridge B. Do not create per-org Paperclip or convert to Tauri sidecar.

## Wave Goal (refined)
Surface all 4 bridges (Vault A, Paperclip singleton B, TeamForge C, Hermes D) in the shell and core screens. Embeddings power relevance. Explicit singleton status for Paperclip everywhere it appears. Build on Phase 2 Wave 2 screen redesign.

## 16 Granular Tasks (ready to turn into next gh batch using the same playbook)

T-2P2-W3-001 | Top-bar persistent bridge health strip (4 bridges + embedding freshness; Paperclip marked "singleton shared instance")
T-2P2-W3-002 | Bridge detail drawer (click any bridge → status, last sync, errors, quick actions; Paperclip drawer explicitly notes one-instance-for-all-orgs)
T-2P2-W3-003 | Vault Intelligence rail extended to show "freshness" and "ownership" badges on every surface (embeddings for ranking)
T-2P2-W3-004 | Paperclip approvals rail (pull from the singleton bridge B instance; surfaced in Overview + Inbox with "shared across orgs" label)
T-2P2-W3-005 | Hermes critical items rail (Telegram/email high-signal, ranked by vault embeddings)
T-2P2-W3-006 | Cloudflare Worker health + last-deploy surface (bridge C)
T-2P2-W3-007 | "Test All Bridges" action that runs the full contract suite (from T-023) and surfaces results, confirming singleton Paperclip is healthy
T-2P2-W3-008 | Global shortcut to open bridge health drawer
T-2P2-W3-009 | Native notification on bridge degradation (with deep link; singleton Paperclip degradation uses the shared instance language)
T-2P2-W3-010 | Dynamic tray now shows per-bridge status (Paperclip = "singleton shared") + top 1-2 vault semantic items
T-2P2-W3-011 | Settings page: full bridge configuration + re-auth + embedding model selector (singleton Paperclip config section)
T-2P2-W3-012 | Command palette gains "bridge: status", "bridge: test", "vault: search <term>" (singleton status visible in results)
T-2P2-W3-013 | Before/after screenshot validation for bridge surfaces (reduced-motion compliant, per creative-frontend-orchestrator)
T-2P2-W3-014 | Performance budget check on new bridge UI elements (load web-perf cluster)
T-2P2-W3-015 | Wave 3 validation gate (all bridges visible + actionable + CLAUDE.md ownership respected + singleton Paperclip model verified)
T-2P2-W3-016 | Wave 3 memory capture + handoff packet to Phase 3 (Full Ecosystem Surfaces) — explicitly record the singleton Paperclip architecture decision

## Notes for future issue creation
- Every task that touches Paperclip must contain the exact constraint text: "Paperclip is a single shared instance (one port-based runtime) serving multiple organizations in the Thoughtseed ecosystem. TeamForge is the operator surface only. No per-org instances and no Tauri sidecar conversion."
- Load `creative-frontend-orchestrator` + relevant Tauri clusters for UI tasks.
- Dependencies on Phase 1 Wave 1 tasks (T-005, T-019, T-023, etc.).
- Total for this wave when filed: 16 additional issues.

## Verification for this wave
- All surfaces must show accurate singleton status for Paperclip.
- No language suggesting multiple Paperclip instances.
- Evidence: screenshots, logs, contract tests.
