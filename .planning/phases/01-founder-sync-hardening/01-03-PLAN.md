---
phase: 01-founder-sync-hardening
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src-tauri/src/vault/parity.rs
  - .planning/phases/01-founder-sync-hardening/01-VERIFICATION.md
  - CHANGELOG.md
  - README.md
  - package.json
  - src-tauri/tauri.conf.json
  - src-tauri/Cargo.toml
  - src-tauri/Cargo.lock
autonomous: false
requirements: [SYNC-01]
must_haves:
  truths:
    - "Fresh Mac with no `node` on PATH can run founder sync from the Settings UI to completion."
    - "The Local Workspace status model still reports `founder_sync_ready` correctly."
    - "Canonical parity is preserved for project briefs, client profiles, onboarding flows, and employee KPI notes (verified by report counts matching v0.1.28 baseline minus the warnings TF-46 will close)."
    - "The OTA-shipped `.app` bundle does not reference any `../scripts/...` repo-checkout fallback path at runtime."
    - "The Cloudflare Worker / D1 wire format is unchanged (out-of-scope per the issue)."
  artifacts:
    - path: ".planning/phases/01-founder-sync-hardening/01-VERIFICATION.md"
      provides: "Phase 1 verification artifact: Tier 1 (fmt/check/test --lib/pnpm build), Tier 2 (clean-PATH founder-sync run on TeamForge.app), Tier 3 (Node-vs-Rust report diff against the real `thoughtseed-labs` vault), Tier 4 (bundle inspection), Tier 5 (release pre-tag). Records exact commands run, exit codes, and the jq-normalized parity diff result."
      contains: "## Tier 2 — Native runtime acceptance"
    - path: "CHANGELOG.md"
      provides: "v0.2.0 entry, mirrors v0.1.28 style: dated header, Changed/Added/Removed sub-blocks, `### Verification` block with the exact commands from this plan."
      contains: "## v0.2.0"
    - path: "package.json"
      provides: "Frontend version bumped from 0.1.28 to 0.2.0."
      contains: "\"version\": \"0.2.0\""
    - path: "src-tauri/tauri.conf.json"
      provides: "Tauri bundle version bumped from 0.1.28 to 0.2.0. Bundle resources entry for the Node script preserved (D-02 dual-path until v0.2.1)."
      contains: "\"version\": \"0.2.0\""
    - path: "src-tauri/Cargo.toml"
      provides: "Cargo crate version bumped from 0.1.28 to 0.2.0."
      contains: "version = \"0.2.0\""
    - path: "README.md"
      provides: "Version banner + `## New In v0.2.0` section + repo-version pointer updated to 0.2.0. Mirrors the v0.1.28 layout at lines :88-151."
      contains: "## New In v0.2.0"
    - path: "src-tauri/src/vault/parity.rs"
      provides: "Adds the `#[ignore]`-gated `rust_parity_diff_against_real_vault` integration test (Tier 3 of `01-RESEARCH.md` §9). Reads `TEAMFORGE_VAULT_ROOT`, `TEAMFORGE_WORKSPACE_ID`, `TEAMFORGE_RUST_PARITY_REPORT_PATH` env vars, calls `run_dry_run` against the real vault, writes the report to the env-specified path. Diff is performed by the human releaser with `jq` (per VERIFICATION.md)."
      contains: "fn rust_parity_diff_against_real_vault"
  key_links:
    - from: "CHANGELOG.md v0.2.0 ### Verification block"
      to: ".planning/phases/01-founder-sync-hardening/01-VERIFICATION.md"
      via: "Both record the same Tier 1-5 command outputs; CHANGELOG is the public summary, VERIFICATION.md is the GSD audit artifact."
      pattern: "01-VERIFICATION\\.md|### Verification"
    - from: "src-tauri/src/vault/parity.rs::rust_parity_diff_against_real_vault"
      to: "scripts/teamforge-vault-parity.mjs (Node baseline output at /tmp/node-report.json)"
      via: "Both runtimes read the same TEAMFORGE_VAULT_ROOT and TEAMFORGE_WORKSPACE_ID; jq-normalized JSON diff verifies parity."
      pattern: "TEAMFORGE_VAULT_ROOT|TEAMFORGE_WORKSPACE_ID"
    - from: "package.json + src-tauri/Cargo.toml + src-tauri/tauri.conf.json"
      to: "git tag v0.2.0"
      via: "All three version strings must match the tag exactly per the v0.1.28 release pattern."
      pattern: "0\\.2\\.0"
---

<objective>
Verify, document, and ship Phase 1. Run the full Verification Ladder from `01-RESEARCH.md` §9 (Tier 1 local correctness, Tier 2 clean-PATH `.app` founder-sync run, Tier 3 Node-vs-Rust report diff against the real `thoughtseed-labs` vault, Tier 4 bundle inspection, Tier 5 release pre-tag), capture results in `01-VERIFICATION.md`, draft the v0.2.0 `CHANGELOG.md` entry mirroring v0.1.28 style, bump versions across `package.json`/`src-tauri/tauri.conf.json`/`src-tauri/Cargo.toml`/`README.md`, and surface the two manual checkpoints (`01-clean-path-app-run`, `01-real-vault-diff` from `01-VALIDATION.md`) for the human releaser.

Purpose: After Plan 01-02 lands, the importer is functionally complete and 10 of 12 VALIDATION.md task IDs are green. The remaining two (`01-real-vault-diff` and `01-clean-path-app-run`) require manual execution by a human at the packaged-app level — they cannot be automated by `cargo test` (per VALIDATION.md §"Manual-Only Verifications" and `tasks/lessons.md:911`). This plan operationalizes those checkpoints, records evidence, and ships the release.

Output:
- `src-tauri/src/vault/parity.rs` — adds ONE `#[ignore]`-gated test (`rust_parity_diff_against_real_vault`) so the Tier 3 diff has a programmatic Rust-side entry point.
- `.planning/phases/01-founder-sync-hardening/01-VERIFICATION.md` — full Tier 1-5 evidence including the parity diff result.
- `CHANGELOG.md` — v0.2.0 entry with `### Verification` block.
- `package.json`, `src-tauri/tauri.conf.json`, `src-tauri/Cargo.toml`, `README.md` — version bumped from 0.1.28 to 0.2.0.
- Plan ends with a release-readiness gate, NOT with `git tag v0.2.0` (tagging is a human action recorded in `tasks/todo.md`'s Review block, mirroring the v0.1.28 release pattern).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-founder-sync-hardening/01-CONTEXT.md
@.planning/phases/01-founder-sync-hardening/01-RESEARCH.md
@.planning/phases/01-founder-sync-hardening/01-VALIDATION.md
@.planning/phases/01-founder-sync-hardening/01-01-SUMMARY.md
@.planning/phases/01-founder-sync-hardening/01-02-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/CONCERNS.md

<interfaces>
<!-- Concrete contracts the executor uses for verification + release. -->

CHANGELOG.md v0.1.28 entry shape (mirror this for v0.2.0):
```
## v0.1.28 - YYYY-MM-DD

<one-paragraph release blurb — what landed and why it matters>

### Changed
- <bullet>

### Added
- <bullet>

### Removed
- <bullet, if anything was removed>

### Verification
- `cargo fmt --manifest-path src-tauri/Cargo.toml`
- `cargo check --manifest-path src-tauri/Cargo.toml`
- `cargo test --manifest-path src-tauri/Cargo.toml --lib`
- `pnpm build`
- Tier 2: clean-PATH founder-sync run on TeamForge.app — PASS
- Tier 3: Node-vs-Rust parity diff against thoughtseed-labs vault — zero diff (or documented diffs in 01-VERIFICATION.md)
```

Version locations to update (4 files, exact strings):
- `package.json:3` — `"version": "0.1.28",` becomes `"version": "0.2.0",`
- `src-tauri/tauri.conf.json:3` — `"version": "0.1.28",` becomes `"version": "0.2.0",`
- `src-tauri/Cargo.toml` — `version = "0.1.28"` becomes `version = "0.2.0"` (the `[package]` block; do NOT touch the `[lib]` block)
- `README.md:88` — `## New In v0.1.28` becomes `## New In v0.2.0` (and the version-pointer lines at :93, :150, :151)

Tier 3 diff command (from RESEARCH.md §9):
```bash
jq -S 'walk(if type == "array" then sort_by(tostring) else . end)
       | del(.. | .lastModifiedAt? // empty)
       | del(.. | .filePath? // empty)
       | del(.. | .reportPath? // empty)' /tmp/node-report.json > /tmp/node-norm.json
# (same for /tmp/rust-report.json -> /tmp/rust-norm.json)
diff /tmp/node-norm.json /tmp/rust-norm.json
```

`#[ignore]`-test pattern (mirrors `commands/mod.rs:11696,11762,11773`):
```rust
#[tokio::test]
#[ignore = "live test — requires TEAMFORGE_VAULT_ROOT + TEAMFORGE_WORKSPACE_ID env vars"]
async fn rust_parity_diff_against_real_vault() { ... }
```

Tauri bundle path for clean-PATH test:
- Built via: `cargo tauri build --bundles app`
- Located at: `src-tauri/target/release/bundle/macos/TeamForge.app`
- Launch via: `open /path/to/TeamForge.app` (NOT direct exec — direct exec inherits parent PATH; per `tasks/lessons.md:911`)
- PATH-strip command: `PATH=$(echo "$PATH" | tr ':' '\n' | grep -v '/node\|/nvm\|/homebrew/bin\|/.local/share/mise' | paste -sd:) which node` — must return empty.

</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add `#[ignore]`-gated `rust_parity_diff_against_real_vault` integration test</name>
  <files>
    src-tauri/src/vault/parity.rs
  </files>

  <read_first>
    - src-tauri/src/vault/parity.rs (after Plan 01-02 — `run_dry_run` is real; this task adds ONE more test at the bottom of the existing `#[cfg(test)] mod tests` block, named exactly `rust_parity_diff_against_real_vault` per `01-VALIDATION.md` row `01-real-vault-diff`)
    - .planning/phases/01-founder-sync-hardening/01-RESEARCH.md §9 Tier 3 (lines 594-625) — the canonical command shape
    - .planning/phases/01-founder-sync-hardening/01-VALIDATION.md row `01-real-vault-diff` — the exact `cargo test` invocation `cargo test --manifest-path src-tauri/Cargo.toml vault::parity::tests::rust_parity_diff_against_real_vault -- --ignored --nocapture`
    - src-tauri/src/commands/mod.rs (any function decorated with `#[ignore]` — search for `#[ignore =` to confirm the existing repo style; per RESEARCH.md §7 the pattern lives at `:11696, :11762, :11773`)
    - src-tauri/src/vault/parity.rs `rust_parity_run_against_fixture_vault` (added in Plan 01-02 Task 3) — this task copies its env-resolution + cleanup pattern but reads vault-root from env instead of CARGO_MANIFEST_DIR
  </read_first>

  <action>
    Append ONE inline test to the existing `#[cfg(test)] mod tests` block at the bottom of `src-tauri/src/vault/parity.rs`. Do not create a new test module. Do not edit Plan 01-02 tests. Test name MUST be exactly `rust_parity_diff_against_real_vault` so the `cargo test` invocation in `01-VALIDATION.md` row `01-real-vault-diff` resolves.

    The test is `#[ignore]`-gated because it requires environment variables that only exist on the human releaser's box (the real `thoughtseed-labs` vault path is private — per CONTEXT.md "Test posture" and VALIDATION.md §"Manual-Only Verifications").

    Concrete shape:

    ```rust
    /// Tier 3 of `01-RESEARCH.md` §9 — parity verification against the real
    /// `thoughtseed-labs` vault. The human releaser runs:
    ///
    ///   TEAMFORGE_VAULT_ROOT=/path/to/thoughtseed-labs \
    ///   TEAMFORGE_WORKSPACE_ID=ws-xxxxxxxx \
    ///   TEAMFORGE_RUST_PARITY_REPORT_PATH=/tmp/rust-report.json \
    ///   cargo test --manifest-path src-tauri/Cargo.toml \
    ///     vault::parity::tests::rust_parity_diff_against_real_vault \
    ///     -- --ignored --nocapture
    ///
    /// then runs the Node script with the same vault + workspace_id and writes
    /// to /tmp/node-report.json, then jq-normalizes both and diffs. The Rust
    /// side just produces the report file at the env-specified path; the diff
    /// itself is recorded in `01-VERIFICATION.md`.
    #[tokio::test]
    #[ignore = "live test — requires TEAMFORGE_VAULT_ROOT + TEAMFORGE_WORKSPACE_ID env vars; not run by `cargo test --lib`"]
    async fn rust_parity_diff_against_real_vault() {
        let vault_root = std::env::var("TEAMFORGE_VAULT_ROOT")
            .expect("TEAMFORGE_VAULT_ROOT env var required for this --ignored test");
        let workspace_id = std::env::var("TEAMFORGE_WORKSPACE_ID")
            .expect("TEAMFORGE_WORKSPACE_ID env var required for this --ignored test");
        let report_path = std::env::var("TEAMFORGE_RUST_PARITY_REPORT_PATH")
            .unwrap_or_else(|_| "/tmp/rust-report.json".to_string());

        eprintln!("[vault-parity] real-vault diff: vault_root={vault_root}");
        eprintln!("[vault-parity] real-vault diff: workspace_id={workspace_id}");
        eprintln!("[vault-parity] real-vault diff: report_path={report_path}");

        // Use the same test-DB pattern as `rust_parity_run_against_fixture_vault`.
        let dir = crate::db::queries::unique_test_dir();
        let pool = crate::db::queries::init_db(&dir).await.expect("init db");

        // Run dry-run against the real vault. No Worker calls. No SQLite writes.
        let report_path_buf = std::path::PathBuf::from(&report_path);
        crate::vault::parity::run_dry_run(
            &pool,
            vault_root.as_str(),
            workspace_id.as_str(),
            "https://teamforge-api.invalid", // dry-run does not hit the worker
            "fake-token",
            &report_path_buf,
        )
        .await
        .expect("run_dry_run against real vault");

        // Sanity: the report file exists and is valid JSON with expected top-level keys.
        let report_text = std::fs::read_to_string(&report_path_buf)
            .expect("read rust report");
        let report: serde_json::Value =
            serde_json::from_str(&report_text).expect("parse rust report as JSON");

        assert_eq!(report["mode"], "dry-run", "report mode must be dry-run");
        assert!(report["counts"].is_object(), "counts must be present");
        assert!(report["warnings"].is_array(), "warnings must be present");
        // The four-family counts the existing parser at commands/mod.rs:2708-2805 reads.
        for key in [
            "projectBriefsFound",
            "clientProfilesFound",
            "onboardingFlowsFound",
            "employeeKpiNotesFound",
        ] {
            assert!(
                report["counts"][key].is_number(),
                "counts.{} must be a number; report did not produce a Node-compatible shape",
                key
            );
        }

        eprintln!(
            "[vault-parity] real-vault diff: rust report written to {} ({} bytes); now run the Node script and `jq`-diff per RESEARCH.md §9 Tier 3.",
            report_path,
            report_text.len()
        );

        // Cleanup the test DB; KEEP the report file at the env-specified path so
        // the human releaser can diff it.
        pool.close().await;
        let _ = std::fs::remove_dir_all(&dir);
    }
    ```

    Critical:
    - `#[ignore = "..."]` is REQUIRED so `cargo test --lib` (Tier 1, Wave 0/1/2 sampling) does NOT execute this. Only the explicit `--ignored` flag enables it. Mirrors the existing pattern at `commands/mod.rs:11696,11762,11773`.
    - The report file at `/tmp/rust-report.json` (or whatever the env override sets) is NOT cleaned up — the human releaser needs it for the `diff` step. The test DB IS cleaned up.
    - Do NOT call into Worker HTTP from this test. The whole point of `run_dry_run` (added in Plan 01-02 Task 2) is that this test does NOT need the network.
    - The test asserts the FOUR family counts exist as numbers; if any are missing, parity drift is caught right here before the human even runs the diff. (Defense in depth around Risk #1 in RESEARCH.md §"Risk Register".)

    Per CONVENTIONS.md:
    - `eprintln!("[vault-parity] …")` for diagnostic output (matches the cadence the rest of the importer uses).
    - `Result<T, String>` is not used here because the test panics on failure via `expect`/`assert` — that is the standard Rust test idiom and matches the rest of `vault::parity::tests`.
  </action>

  <verify>
    <automated>cargo fmt --manifest-path src-tauri/Cargo.toml --check &amp;&amp; cargo check --manifest-path src-tauri/Cargo.toml &amp;&amp; cargo test --manifest-path src-tauri/Cargo.toml --lib &amp;&amp; cargo test --manifest-path src-tauri/Cargo.toml vault::parity::tests::rust_parity_diff_against_real_vault -- --list 2&gt;&amp;1 | grep -q 'rust_parity_diff_against_real_vault'</automated>
  </verify>

  <acceptance_criteria>
    - `grep -q 'fn rust_parity_diff_against_real_vault' src-tauri/src/vault/parity.rs` exits 0.
    - `grep -q '#\[ignore' src-tauri/src/vault/parity.rs` exits 0 (the test is gated; matches the repo's existing `#[ignore]` style).
    - `grep -q 'TEAMFORGE_VAULT_ROOT' src-tauri/src/vault/parity.rs` exits 0 (env var read).
    - `grep -q 'TEAMFORGE_WORKSPACE_ID' src-tauri/src/vault/parity.rs` exits 0.
    - `grep -q 'TEAMFORGE_RUST_PARITY_REPORT_PATH' src-tauri/src/vault/parity.rs` exits 0 (overridable report path).
    - `grep -q 'run_dry_run' src-tauri/src/vault/parity.rs` exits 0 (test invokes the dry-run entry point, not run_apply).
    - `cargo fmt --manifest-path src-tauri/Cargo.toml --check` exits 0.
    - `cargo check --manifest-path src-tauri/Cargo.toml` exits 0 with zero warnings (`2>&1 | grep -c '^warning:'` returns 0).
    - `cargo test --manifest-path src-tauri/Cargo.toml --lib` exits 0 — the new test is `#[ignore]`'d so does not run; the rest of the suite (incl. the 10 from Plan 01-02) stays green.
    - `cargo test --manifest-path src-tauri/Cargo.toml vault::parity::tests::rust_parity_diff_against_real_vault -- --list 2>&1 | grep -q 'rust_parity_diff_against_real_vault'` exits 0 (the test is registered, just gated).
    - `cargo test --manifest-path src-tauri/Cargo.toml vault::parity::tests --` reports >= 11 tests (10 from Plan 01-02 + 1 ignored).
  </acceptance_criteria>

  <done>
    The Rust side of the Tier 3 parity diff is in place: a single `#[ignore]`-gated test that the human releaser invokes with env vars to produce `/tmp/rust-report.json`. The 11th VALIDATION.md task ID (`01-real-vault-diff`) is now structurally available — it executes only when the human runs Tier 3. `cargo test --lib` baseline remains green; no new compile warnings.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Run Tier 1 verification + bump versions to 0.2.0 across all four files</name>
  <files>
    package.json,
    src-tauri/tauri.conf.json,
    src-tauri/Cargo.toml,
    src-tauri/Cargo.lock,
    README.md
  </files>

  <read_first>
    - package.json (current `"version": "0.1.28"` at line :3)
    - src-tauri/tauri.conf.json (current `"version": "0.1.28"` at line :3 — the `productName` and `identifier` lines stay)
    - src-tauri/Cargo.toml (current `version = "0.1.28"` in the `[package]` block at the top — the `[lib]` block has its own `name = "team_forge_lib"` and must NOT be touched)
    - src-tauri/Cargo.lock (auto-rewritten by cargo on next build — read so the executor knows it's a regenerated artifact, not a hand-edited one)
    - README.md (the v0.1.28 banner section at lines :88-151 — mirror its structure for v0.2.0; specifically lines :88 `## New In v0.1.28`, :93 release-metadata pointer, :150-151 current-version pointers)
    - CHANGELOG.md (read the full v0.1.28 entry at lines :5-end to learn the structure that the v0.2.0 entry in Task 3 must mirror)
    - .planning/phases/01-founder-sync-hardening/01-RESEARCH.md §9 Tier 5 (lines 645-654) — the exact pre-tag command list
  </read_first>

  <action>
    Three sub-steps. Run them in order; do not interleave.

    Sub-step A — Tier 1 verification gate (must pass before bumping versions). Run:

    ```bash
    cargo fmt --manifest-path src-tauri/Cargo.toml --check
    cargo check --manifest-path src-tauri/Cargo.toml
    test $(cargo check --manifest-path src-tauri/Cargo.toml 2>&1 | grep -c '^warning:') -eq 0
    cargo test --manifest-path src-tauri/Cargo.toml --lib
    pnpm build
    git diff --check
    ```

    All six commands MUST exit 0 before continuing. If any fails, halt and report — Tier 1 is the gate; version bumps cannot happen on a red build. Record the test-count summary line from `cargo test --lib` (e.g. `test result: ok. 53 passed; 0 failed; 3 ignored`) — this becomes evidence in Task 4's VERIFICATION.md.

    Sub-step B — Bump versions across the four canonical version files. All bumps are 0.1.28 to 0.2.0.

    1. `package.json` line :3 — change `"version": "0.1.28",` to `"version": "0.2.0",`. Do NOT touch any other field.

    2. `src-tauri/tauri.conf.json` line :3 — change `"version": "0.1.28",` to `"version": "0.2.0",`. The `productName: "TeamForge"` and `identifier: "com.thoughtseed.teamforge"` lines stay. The `bundle.resources` entry for `teamforge-vault-parity.mjs` STAYS in v0.2.0 — D-02 dual-path; it goes out at v0.2.1.

    3. `src-tauri/Cargo.toml` `[package]` block — change `version = "0.1.28"` to `version = "0.2.0"`. The `name = "team-forge-ts"` line stays. The `[lib]` block's `name = "team_forge_lib"` MUST NOT be touched.

    4. `README.md`:
       - Line :88 — change `## New In v0.1.28` to `## New In v0.2.0`.
       - Line :93 — change ``Release metadata is now at `0.1.28``` to ``Release metadata is now at `0.2.0```.
       - Line :150 — change ``Current app version in repo: `0.1.28``` to ``Current app version in repo: `0.2.0```.
       - Line :151 — change ``Latest published tag: `v0.1.28``` to ``Latest published tag: `v0.2.0```.
       - Line :226 (the Cloudflare project-registry tranche pointer) — leave as `0.1.28` ONLY IF the surrounding sentence describes the v0.1.28 line specifically (it does: "is now implemented on the current `0.1.28` line"). The executor confirms by reading the surrounding paragraph; if the prose has been updated for v0.2 in the meantime, update the pointer to `0.2.0`.
       - Replace the body of the `## New In v0.2.0` section with a 5-7 bullet summary mirroring the v0.1.28 layout. The bullets must reference: native Rust importer at `src-tauri/src/vault/parity.rs` (D-01); `vault_sync_runtime` setting with default `rust` and Node fallback for one release (D-02); full parity for all four note families (D-03); preserved JSON-on-disk report contract (D-04); clean-PATH founder-sync verified on macOS (Tier 2); and the `gray_matter` dep addition. Use the prose from CONTEXT.md `<decisions>` as the source of truth for what changed.

    5. `src-tauri/Cargo.lock` — regenerated automatically by the next `cargo check` run. After Sub-step B-3, run:

       ```bash
       cargo check --manifest-path src-tauri/Cargo.toml
       ```

       The lock file's `team-forge-ts` package version line will update from `0.1.28` to `0.2.0`. Do NOT hand-edit `Cargo.lock`.

    Sub-step C — Re-run Tier 1 to confirm the bumps did not break anything. Identical to Sub-step A:

    ```bash
    cargo fmt --manifest-path src-tauri/Cargo.toml --check
    cargo check --manifest-path src-tauri/Cargo.toml
    cargo test --manifest-path src-tauri/Cargo.toml --lib
    pnpm build
    git diff --check
    ```

    All five must exit 0. The diff at this point covers ALL four version files plus `Cargo.lock` regeneration only.

    Per CONVENTIONS.md and the v0.1.28 release pattern:
    - The version string `0.2.0` MUST be identical across all four hand-edited files (sentinel for the eventual `git tag v0.2.0`).
    - `Cargo.lock` is committed (CONCERNS.md §"Cargo.lock" — the repo commits it for binary reproducibility).
    - `package.json` and `src-tauri/tauri.conf.json` use SemVer string format `"X.Y.Z"`; `Cargo.toml` uses `version = "X.Y.Z"`.
  </action>

  <verify>
    <automated>cargo fmt --manifest-path src-tauri/Cargo.toml --check &amp;&amp; cargo check --manifest-path src-tauri/Cargo.toml &amp;&amp; cargo test --manifest-path src-tauri/Cargo.toml --lib &amp;&amp; pnpm build &amp;&amp; git diff --check &amp;&amp; grep -q '"version": "0.2.0"' package.json &amp;&amp; grep -q '"version": "0.2.0"' src-tauri/tauri.conf.json &amp;&amp; grep -q 'version = "0.2.0"' src-tauri/Cargo.toml &amp;&amp; grep -q 'New In v0.2.0' README.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c '"version": "0.2.0"' package.json` returns 1 (exact-match line in the JSON top-level).
    - `grep -c '"version": "0.2.0"' src-tauri/tauri.conf.json` returns 1.
    - `grep -c '^version = "0.2.0"' src-tauri/Cargo.toml` returns 1 (the `[package]` block line).
    - `grep -c '0\.1\.28' package.json` returns 0 (no leftover old version).
    - `grep -c '0\.1\.28' src-tauri/tauri.conf.json` returns 0.
    - `grep -c '0\.1\.28' src-tauri/Cargo.toml` returns 0.
    - `grep -q '## New In v0.2.0' README.md` exits 0.
    - `grep -q 'Release metadata is now at .0\.2\.0' README.md` exits 0.
    - `grep -q 'Current app version in repo: .0\.2\.0' README.md` exits 0.
    - `grep -q 'Latest published tag: .v0\.2\.0' README.md` exits 0.
    - `cargo fmt --manifest-path src-tauri/Cargo.toml --check` exits 0.
    - `cargo check --manifest-path src-tauri/Cargo.toml` exits 0 with zero warnings.
    - `cargo test --manifest-path src-tauri/Cargo.toml --lib` exits 0 (no regression).
    - `pnpm build` exits 0 (frontend TypeScript still compiles).
    - `git diff --check` exits 0 (no whitespace damage).
    - `grep 'name = "team-forge-ts"' -A 1 src-tauri/Cargo.toml | grep -q 'version = "0.2.0"'` exits 0 (the [package] block was the one updated, not [lib]).
    - `grep -q 'name = "team_forge_lib"' src-tauri/Cargo.toml` exits 0 (the [lib] block is untouched — it has no version field).
    - `grep -q '"team-forge-ts"' src-tauri/Cargo.lock && grep -A 1 '"team-forge-ts"' src-tauri/Cargo.lock | grep -q '0\.2\.0'` exits 0 (Cargo.lock regenerated).
  </acceptance_criteria>

  <done>
    All four canonical version files report `0.2.0`. `Cargo.lock` regenerated to match. Tier 1 verification (fmt/check/test --lib/pnpm build/git diff --check) is green both BEFORE and AFTER the bumps. The README's `## New In v0.2.0` section captures Phase 1's user-visible changes. The repo is ready for the manual checkpoints in Tasks 3 and 4.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual checkpoint — Tier 2 (clean-PATH .app run) + Tier 3 (Node-vs-Rust parity diff)</name>
  <files>
    .planning/phases/01-founder-sync-hardening/01-VERIFICATION.md
  </files>

  <what-built>
    Tasks 1-2 produced:
    - `src-tauri/src/vault/parity.rs` — full native Rust importer + 11 inline tests (10 unit/integration green via `cargo test --lib`, 1 `#[ignore]`-gated for live real-vault diff).
    - All four version files at `0.2.0`.
    - Tier 1 verification (fmt/check/test --lib/pnpm build) green.

    What this checkpoint exists to verify:
    - **Tier 2:** TeamForge.app runs founder vault sync end-to-end on a clean-PATH macOS install with no `node` available. THIS IS THE LITERAL ACCEPTANCE CRITERION OF ISSUE #45 (per VALIDATION.md row `01-clean-path-app-run`).
    - **Tier 3:** Rust path produces a JSON parity report that, when jq-normalized, matches the Node script's output against the same vault. Regression-locks D-04.
  </what-built>

  <how-to-verify>
    Two sub-procedures. Run BOTH; record both results in `01-VERIFICATION.md` (Task 4 then converts those notes into the final structured artifact).

    Sub-procedure A — Tier 2 (clean-PATH .app run, ~10 min):

    1. Build the .app bundle from the repo root:
       ```bash
       cargo tauri build --bundles app
       ```
       Expected: TeamForge.app produced under `src-tauri/target/release/bundle/macos/TeamForge.app`. (Signing-skip is fine; this is a verify-by-execution test, not a distribution build.)

    2. Confirm the bundle's product version is 0.2.0:
       ```bash
       /usr/libexec/PlistBuddy -c "Print :CFBundleShortVersionString" \
         src-tauri/target/release/bundle/macos/TeamForge.app/Contents/Info.plist
       ```
       Expected: prints `0.2.0`. If `0.1.28`, Task 2's bumps did not propagate — halt.

    3. In a NEW terminal, strip Node from PATH and verify:
       ```bash
       PATH=$(echo "$PATH" | tr ':' '\n' | grep -v '/node\|/nvm\|/homebrew/bin\|/.local/share/mise' | paste -sd:)
       which node
       ```
       Expected: `which node` returns nothing (exit 1, empty stdout). If `node` is found, the PATH-strip pattern needs to be widened for this shell — adapt and rerun until `which node` is empty.

    4. From that same NEW terminal, launch the .app via LaunchServices (NOT direct exec — direct exec inherits the stripped PATH but other shell side-effects can leak; per `tasks/lessons.md:911`):
       ```bash
       open src-tauri/target/release/bundle/macos/TeamForge.app
       ```

    5. In the launched TeamForge window:
       - Open Settings → Local Workspace block.
       - Confirm `founder_sync_ready` shows green / "Ready" (per Success Criterion #2).
       - Click "Sync vault" (or whatever the existing button label is — confirm against `src/pages/Settings.tsx`).
       - Wait for the sync to complete.
       - Expected: no error toast; status returns to "ready"; `Last synced` timestamp updates.
       - Open the Agent Feed (or whatever surface lists vault-sourced rows) and confirm new rows appear matching what the v0.1.28 Node-path baseline produced.

    6. Optional but recommended — verify the rust path actually ran (not the Node fallback):
       ```bash
       sqlite3 ~/Library/Application\ Support/com.thoughtseed.teamforge/teamforge.db \
         "SELECT key, value FROM settings WHERE key = 'vault_sync_runtime';"
       ```
       Expected: either no row (default = "rust") or one row with `value = "rust"`. If `value = "node"`, the dual-path safety belt is active and Tier 2 was tested via the wrong path — clear the row and re-sync.

    7. Bundle inspection (Tier 4, lightweight — fold into this checkpoint):
       ```bash
       find src-tauri/target/release/bundle/macos/TeamForge.app -name "*.mjs" 2>/dev/null
       ```
       Expected (v0.2.0): `teamforge-vault-parity.mjs` is found (D-02 dual-path safety net STAYS bundled until v0.2.1). At v0.2.1 this becomes the "expected: empty" check.

    Sub-procedure B — Tier 3 (Node-vs-Rust parity diff against real vault, ~15 min):

    1. Set env vars (the human releaser knows the local paths):
       ```bash
       export THOUGHTSEED_VAULT_ROOT=/path/to/your/thoughtseed-labs
       export TF_WORKSPACE_ID=ws-xxxxxxxx   # from your TeamForge settings
       ```

    2. Run the Node baseline path with `--local-only --apply=false` (no writes, just produces the report):
       ```bash
       node scripts/teamforge-vault-parity.mjs \
         --local-only --apply=false \
         --vault-root "$THOUGHTSEED_VAULT_ROOT" \
         --workspace-id "$TF_WORKSPACE_ID" \
         --report /tmp/node-report.json
       ```
       Expected: `/tmp/node-report.json` exists; ~50-200 KB JSON.

    3. Run the Rust path via the `#[ignore]`-gated test from Task 1:
       ```bash
       TEAMFORGE_VAULT_ROOT="$THOUGHTSEED_VAULT_ROOT" \
       TEAMFORGE_WORKSPACE_ID="$TF_WORKSPACE_ID" \
       TEAMFORGE_RUST_PARITY_REPORT_PATH=/tmp/rust-report.json \
       cargo test --manifest-path src-tauri/Cargo.toml \
         vault::parity::tests::rust_parity_diff_against_real_vault \
         -- --ignored --nocapture
       ```
       Expected: test passes; `/tmp/rust-report.json` exists; `cargo test` output reports `1 passed; 0 failed; 0 ignored` (the rest of the suite is excluded by the explicit test path).

    4. Normalize and diff (the jq expression strips lastModifiedAt timestamps and absolute file paths that legitimately differ between runs):
       ```bash
       jq -S 'walk(if type == "array" then sort_by(tostring) else . end)
              | del(.. | .lastModifiedAt? // empty)
              | del(.. | .filePath? // empty)
              | del(.. | .reportPath? // empty)' /tmp/node-report.json > /tmp/node-norm.json

       jq -S 'walk(if type == "array" then sort_by(tostring) else . end)
              | del(.. | .lastModifiedAt? // empty)
              | del(.. | .filePath? // empty)
              | del(.. | .reportPath? // empty)' /tmp/rust-report.json > /tmp/rust-norm.json

       diff /tmp/node-norm.json /tmp/rust-norm.json
       ```

       Expected outcomes (in order of preference):
       - **Best:** Zero diff (the two files are byte-identical after normalization). Phase 1 done. Record "zero diff" in 01-VERIFICATION.md.
       - **Acceptable:** Diffs limited to: integer 5 vs string "5" (rare; document if seen), known-deferred fields (e.g. fields the Node path emits but TF-46 explicitly defers — list in 01-VERIFICATION.md), or Rust path emits a strict superset (additional `warnings` entries flagged for TF-46). Phase 1 done with documented diffs.
       - **Block ship:** Counts disagree (`projectBriefsFound`, `clientProfilesFound`, `onboardingFlowsFound`, `employeeKpiNotesFound`); `creates`/`updates` disagree non-trivially; any of the four `*Failures` arrays exists in one report and not the other; `mode` field missing or differently typed. → Halt; file an issue; revisit Plan 01-02 implementation.

    5. Note the diff output (or "zero diff" confirmation) — Task 4 incorporates it into 01-VERIFICATION.md.

    Per CONTEXT.md `<specifics>` "Spec preservation": if the diff reveals behavior the Rust path doesn't match, FLAG it in 01-VERIFICATION.md rather than silently fixing it. The user calls the next move (file a follow-up issue / patch in this phase / accept the diff).
  </how-to-verify>

  <resume-signal>
    Reply with one of:
    - `tier-2 PASS, tier-3 zero-diff` — best case; proceed to Task 4.
    - `tier-2 PASS, tier-3 documented-diff: <one-line summary>` — proceed to Task 4 with the diff captured.
    - `tier-2 FAIL: <description>` — halt; describe the failure mode and we'll diagnose.
    - `tier-3 FAIL: <description>` — halt; counts or shape diverge; we'll triage which Plan 01-02 task needs revisit.
  </resume-signal>

  <action>
    This is a `checkpoint:human-verify` task. The verifier (human releaser) executes the Tier 2 and Tier 3 procedures specified in `<how-to-verify>` above and replies with the resume-signal token. No autonomous Claude action — this gate exists because Tauri WebView IPC + macOS LaunchServices behavior cannot be exercised by `cargo test`, and the real `thoughtseed-labs` vault is private and cannot be checked into fixtures. Notes from this checkpoint feed Task 4's VERIFICATION.md write.
  </action>

  <verify>
    <automated>MISSING — Tier 2 (clean-PATH .app run) and Tier 3 (Node-vs-Rust real-vault parity diff) cannot be exercised by `cargo test` per `01-VALIDATION.md` §"Manual-Only Verifications". The verifier's reply text (one of the four resume-signal tokens) is the verification artifact; Task 4 records it in `01-VERIFICATION.md`.</automated>
  </verify>

  <done>
    Verifier replied with one of the four `resume-signal` tokens. The reply text is captured by the executor for verbatim inclusion in Task 4's `01-VERIFICATION.md` Tier 2 + Tier 3 sections. If `tier-2 FAIL` or `tier-3 FAIL`, the plan halts before Task 4 and the failure is triaged against Plan 01-01 / 01-02 as appropriate.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Write 01-VERIFICATION.md and CHANGELOG.md v0.2.0 entry</name>
  <files>
    .planning/phases/01-founder-sync-hardening/01-VERIFICATION.md,
    CHANGELOG.md
  </files>

  <read_first>
    - .planning/phases/01-founder-sync-hardening/01-RESEARCH.md §"Verification Ladder" lines 547-660 (the canonical Tier 1-5 structure)
    - .planning/phases/01-founder-sync-hardening/01-VALIDATION.md (the Per-Task Verification Map; this artifact references which task IDs landed green)
    - CHANGELOG.md (v0.1.28 entry at lines :5-end — the structure to mirror)
    - .planning/STATE.md (record the Phase 1 completion in the session continuity section — but only via the SUMMARY in `<output>`, not in this task)
    - The Task 3 checkpoint reply (the human releaser's resume-signal text) — that is the source of truth for Tier 2 + Tier 3 outcomes recorded in this task.
  </read_first>

  <action>
    Two file writes. Both reference Tier 1-5 outcomes from Tasks 2 and 3.

    File 1: `.planning/phases/01-founder-sync-hardening/01-VERIFICATION.md` — fresh write (file does not exist yet). Use this exact structure:

    ```markdown
    ---
    phase: 01-founder-sync-hardening
    requirement: SYNC-01
    status: complete    # or "documented-diffs" or "blocked" — set per Task 3 outcome
    date: YYYY-MM-DD    # the date the verification was run
    verifier: claude-code   # the agent or human who recorded this
    ---

    # Phase 1 — Verification Artifact

    > Records the full Verification Ladder run before tagging v0.2.0. Mirrors `01-RESEARCH.md` §9.

    ## Tier 1 — Local Correctness

    | Step | Command | Exit Code | Output Summary |
    |------|---------|-----------|----------------|
    | 1 | `cargo fmt --manifest-path src-tauri/Cargo.toml --check` | 0 | clean |
    | 2 | `cargo check --manifest-path src-tauri/Cargo.toml` | 0 | 0 errors, 0 warnings |
    | 3 | `cargo test --manifest-path src-tauri/Cargo.toml --lib` | 0 | `test result: ok. <N> passed; 0 failed; >= 3 ignored` |
    | 4 | `pnpm build` | 0 | TypeScript clean, Vite emitted dist/ |
    | 5 | `git diff --check` | 0 | no whitespace damage |

    Test count delta vs v0.1.28 baseline: +11 tests in `vault::parity::tests` (10 active + 1 ignored). Total `cargo test --lib` count: <fill in from Task 2 Sub-step A>.

    VALIDATION.md task IDs marked green at this tier:
    - `01-frontmatter-parser`, `01-status-normalize`, `01-workspace-id-fallback`, `01-request-body-dup-keys`, `01-merge-artifacts`, `01-kpi-employee-resolve`, `01-onboarding-apply-guard`, `01-report-struct-shape`, `01-kpi-snapshot-sqlite`, `01-fixture-vault-parity` — all green via the standard `cargo test --lib` pass.

    ## Tier 2 — Native Runtime Acceptance (THE acceptance criterion for issue #45)

    Verifier recorded: <PASS | FAIL — paste resume-signal from Task 3 Sub-procedure A>

    Steps run (per `01-RESEARCH.md` §9 Tier 2):
    1. `cargo tauri build --bundles app` — produced `src-tauri/target/release/bundle/macos/TeamForge.app` at version `0.2.0`.
    2. PATH stripped of node/nvm/homebrew/mise; `which node` returned empty.
    3. `open src-tauri/target/release/bundle/macos/TeamForge.app`.
    4. Settings → Local Workspace → "Sync vault" — sync ran to completion; UI showed non-zero project_briefs_found / client_profiles_found / onboarding_flows_found; no error toast.
    5. SQLite check: `vault_sync_runtime` setting was either absent (default rust) or set to "rust" — confirming the Rust path was exercised.

    VALIDATION.md task ID marked green at this tier: `01-clean-path-app-run`.

    ## Tier 3 — Parity Verification (Rust output matches Node output)

    Vault: `<THOUGHTSEED_VAULT_ROOT>` (private; not checked in).
    Workspace: `<TF_WORKSPACE_ID>` (private; not checked in).

    Reports:
    - Node: `/tmp/node-report.json` (size: <bytes>)
    - Rust: `/tmp/rust-report.json` (size: <bytes>)

    Normalized via `jq` per RESEARCH.md §9 Tier 3 (sort arrays, drop lastModifiedAt/filePath/reportPath).

    Result: <"zero diff" | "documented diffs below">

    ### Parity Diff Results (only if diffs exist)

    | Field | Node Output | Rust Output | Resolution |
    |-------|-------------|-------------|------------|
    | <jq path> | <node value> | <rust value> | <accept / fix in TF-46 / file follow-up> |

    VALIDATION.md task ID marked green at this tier: `01-real-vault-diff`.

    ## Tier 4 — Bundle Inspection

    | Check | Command | Result |
    |-------|---------|--------|
    | Node script bundled (D-02 safety net) | `find src-tauri/target/release/bundle/macos/TeamForge.app -name "*.mjs"` | finds `teamforge-vault-parity.mjs` (expected v0.2.0; will be empty at v0.2.1) |
    | Bundle product version | `PlistBuddy ... CFBundleShortVersionString` | `0.2.0` |
    | Bundle identifier | `PlistBuddy ... CFBundleIdentifier` | `com.thoughtseed.teamforge` |

    ## Tier 5 — Release Pre-Tag

    Re-ran the Tier 1 commands after Tasks 2 and 4 to confirm no regression. All exit 0.

    Files at v0.2.0 (sentinel for `git tag v0.2.0`):
    - `package.json` line :3
    - `src-tauri/tauri.conf.json` line :3
    - `src-tauri/Cargo.toml` `[package]` block
    - `src-tauri/Cargo.lock` `team-forge-ts` package
    - `README.md` lines :88, :93, :150, :151

    ## Manual-Only Verification Sign-Off

    | Behavior | Verifier | Date | Result |
    |----------|----------|------|--------|
    | Clean-PATH founder-sync run | <human> | <date> | PASS / FAIL |
    | Node-vs-Rust parity diff against real vault | <human> | <date> | zero diff / documented diffs |

    ## Locked Decisions Realized

    - **D-01** (native Rust importer): `src-tauri/src/vault/parity.rs` ships ~1200-1500 LOC; replaces `node scripts/teamforge-vault-parity.mjs` as the default. Verified by Tier 2.
    - **D-02** (dual-path with v0.2.1 kill date): `vault_sync_runtime` setting wired in `commands/mod.rs`; default `"rust"`; `"node"` falls back to the bundled script (still in `tauri.conf.json:bundle.resources`). Kill at v0.2.1 after Phase 2 verifies parity against real data.
    - **D-03** (full parity, all 4 note families): project briefs, client profiles, onboarding flows, employee KPI notes all ported. Verified by Tier 3 zero-diff (or documented diffs).
    - **D-04** (preserve JSON-on-disk report contract): `commands/mod.rs:2708-2805` parser unchanged; Rust producer emits Node-compatible shape. Regression-locked by `report_struct_serializes_to_node_compatible_json` test.
    - **D-05** (straight to stable): no beta channel. v0.2.0 ships to stable.
    - **D-06** (cross-AI peer review): captured in `01-REVIEWS.md` between `/gsd:plan-phase 1` and `/gsd:execute-phase 1`. Reviewers: <Gemini, Codex>. Outcome: <consensus / divergent views recorded>.

    ## Sign-Off

    - [ ] All five tiers passed (or documented diffs accepted).
    - [ ] CHANGELOG.md v0.2.0 entry written and matches this artifact's `### Verification` block.
    - [ ] All four version files at 0.2.0; `Cargo.lock` regenerated.
    - [ ] Ready for `git tag v0.2.0` (executed by the human releaser per the v0.1.28 release pattern, NOT by this plan).
    ```

    File 2: `CHANGELOG.md` — INSERT a new top-of-file entry above the existing `## v0.1.28 - 2026-05-01` block. Do NOT edit the v0.1.28 entry. Use this structure (mirrors v0.1.28's exact heading hierarchy and bullet style):

    ```markdown
    ## v0.2.0 - YYYY-MM-DD

    Closes Phase 1 of v0.2 Foundation Closeout (issue #45). Productizes the
    Settings-based founder vault sync so TeamForge.app runs end-to-end on a
    clean macOS install with no `node` on PATH. The previous Node script
    (~2778 LOC at `scripts/teamforge-vault-parity.mjs`) is replaced as the
    default by a native Rust importer at `src-tauri/src/vault/parity.rs`;
    the script remains bundled for one release as a `vault_sync_runtime`
    setting fallback and goes out at v0.2.1 alongside Phase 2 (#46) closing.

    ### Added

    - Native Rust founder-sync importer at `src-tauri/src/vault/parity.rs`
      (~<LOC>) covering all four note families: project briefs, client
      profiles, onboarding flows, employee KPI notes.
    - `vault_sync_runtime` setting (`"rust"` (default) | `"node"`) at the
      `sync_local_vault_to_teamforge` Tauri command. Manual override via SQL
      against the local `settings` table is the documented v0.2.0 escape
      hatch; no Settings UI surface in this release (deferred per CONTEXT.md
      "Claude's Discretion").
    - `gray_matter = "0.3"` (yaml feature only) — pure-Rust, MIT, zero C
      deps. Single new dependency.
    - 11 inline tests in `vault::parity::tests` (10 unit/integration green
      by default, 1 `#[ignore]`-gated for live real-vault parity diff).
    - Wave 0 fixture vault at `src-tauri/tests-fixtures/vault-min/` (7
      minimal markdown files) for the integration test.

    ### Changed

    - `src-tauri/src/vault.rs` (1163 LOC) moved byte-identical to
      `src-tauri/src/vault/mod.rs` to make room for `vault/parity.rs`. No
      public API change. No call-site change.
    - `commands/mod.rs::sync_local_vault_to_teamforge` now dispatches via
      `match runtime_choice` between the new Rust path and the existing
      Node shell-out. The downstream JSON parser at `:2708-2805` is
      unchanged — D-04 preserves the on-disk report contract; only the
      producer changes.
    - `commands/mod.rs::read_local_workspace_status` no longer surfaces
      Node-specific blockers (`node_runtime_error`, `parity_script_error`)
      when `vault_sync_runtime == "rust"`.

    ### Removed

    - Nothing in this release. The Node script and the dual-path setting
      remain bundled until v0.2.1 alongside Phase 2 (#46) closing.

    ### Verification

    - `cargo fmt --manifest-path src-tauri/Cargo.toml`
    - `cargo check --manifest-path src-tauri/Cargo.toml` — 0 errors, 0 warnings
    - `cargo test --manifest-path src-tauri/Cargo.toml --lib` — `test result: ok. <N> passed; 0 failed; >= 3 ignored` (+11 vs v0.1.28 baseline)
    - `pnpm build`
    - Tier 2: clean-PATH founder-sync run on TeamForge.app — <PASS | FAIL>
    - Tier 3: Node-vs-Rust parity diff against thoughtseed-labs vault — <zero diff | documented diffs in `.planning/phases/01-founder-sync-hardening/01-VERIFICATION.md`>
    - Full audit trail: `.planning/phases/01-founder-sync-hardening/01-VERIFICATION.md`
    ```

    Substitute `<LOC>`, `<N>`, `<PASS | FAIL>`, `<zero diff | ...>`, and the YYYY-MM-DD date with the actual values from Tasks 1, 2, and 3.

    Per CONVENTIONS.md and the v0.1.28 release pattern:
    - The new entry goes at the TOP of CHANGELOG.md, above the v0.1.28 block.
    - The `### Verification` block uses inline backticks for commands and bullet points for results.
    - The blurb prose mirrors v0.1.28's: 1-paragraph context + Added/Changed/Removed/Verification sub-blocks.
  </action>

  <verify>
    <automated>test -f .planning/phases/01-founder-sync-hardening/01-VERIFICATION.md &amp;&amp; grep -q '## Tier 2' .planning/phases/01-founder-sync-hardening/01-VERIFICATION.md &amp;&amp; grep -q '## Tier 3' .planning/phases/01-founder-sync-hardening/01-VERIFICATION.md &amp;&amp; grep -q '^## v0.2.0' CHANGELOG.md &amp;&amp; grep -q '^## v0.1.28' CHANGELOG.md &amp;&amp; head -3 CHANGELOG.md | grep -q '^# Changelog' &amp;&amp; awk '/^## v0\.2\.0/{p=1} /^## v0\.1\.28/{exit} p' CHANGELOG.md | grep -q '### Verification'</automated>
  </verify>

  <acceptance_criteria>
    - `test -f .planning/phases/01-founder-sync-hardening/01-VERIFICATION.md` exits 0.
    - `grep -q '^---$' .planning/phases/01-founder-sync-hardening/01-VERIFICATION.md` exits 0 (frontmatter present).
    - `grep -q 'phase: 01-founder-sync-hardening' .planning/phases/01-founder-sync-hardening/01-VERIFICATION.md` exits 0.
    - `grep -q 'requirement: SYNC-01' .planning/phases/01-founder-sync-hardening/01-VERIFICATION.md` exits 0.
    - `grep -q '## Tier 1' .planning/phases/01-founder-sync-hardening/01-VERIFICATION.md` exits 0.
    - `grep -q '## Tier 2' .planning/phases/01-founder-sync-hardening/01-VERIFICATION.md` exits 0.
    - `grep -q '## Tier 3' .planning/phases/01-founder-sync-hardening/01-VERIFICATION.md` exits 0.
    - `grep -q '## Tier 4' .planning/phases/01-founder-sync-hardening/01-VERIFICATION.md` exits 0.
    - `grep -q '## Tier 5' .planning/phases/01-founder-sync-hardening/01-VERIFICATION.md` exits 0.
    - `grep -q 'D-01' .planning/phases/01-founder-sync-hardening/01-VERIFICATION.md` exits 0 (locked decisions traceability).
    - `grep -q 'D-02' .planning/phases/01-founder-sync-hardening/01-VERIFICATION.md` exits 0.
    - `grep -q 'D-03' .planning/phases/01-founder-sync-hardening/01-VERIFICATION.md` exits 0.
    - `grep -q 'D-04' .planning/phases/01-founder-sync-hardening/01-VERIFICATION.md` exits 0.
    - `grep -q 'D-05' .planning/phases/01-founder-sync-hardening/01-VERIFICATION.md` exits 0.
    - `grep -q 'D-06' .planning/phases/01-founder-sync-hardening/01-VERIFICATION.md` exits 0.
    - `grep -q '01-clean-path-app-run' .planning/phases/01-founder-sync-hardening/01-VERIFICATION.md` exits 0 (manual checkpoint resolved).
    - `grep -q '01-real-vault-diff' .planning/phases/01-founder-sync-hardening/01-VERIFICATION.md` exits 0.
    - `head -3 CHANGELOG.md | grep -q '^# Changelog'` exits 0 (top-of-file header preserved).
    - `grep -n '^## v0\.2\.0' CHANGELOG.md | head -1 | awk -F: '{print $1}' | xargs -I{} test {} -lt 20` exits 0 (v0.2.0 entry appears in the first 20 lines = above the v0.1.28 entry).
    - `grep -q '^## v0\.1\.28' CHANGELOG.md` exits 0 (v0.1.28 entry preserved).
    - `awk '/^## v0\.2\.0/{p=1} /^## v0\.1\.28/{exit} p' CHANGELOG.md | grep -q '### Added'` exits 0 (Added block in v0.2.0).
    - `awk '/^## v0\.2\.0/{p=1} /^## v0\.1\.28/{exit} p' CHANGELOG.md | grep -q '### Changed'` exits 0.
    - `awk '/^## v0\.2\.0/{p=1} /^## v0\.1\.28/{exit} p' CHANGELOG.md | grep -q '### Verification'` exits 0.
    - `awk '/^## v0\.2\.0/{p=1} /^## v0\.1\.28/{exit} p' CHANGELOG.md | grep -q 'cargo test'` exits 0 (Verification block contains the canonical commands).
    - `awk '/^## v0\.2\.0/{p=1} /^## v0\.1\.28/{exit} p' CHANGELOG.md | grep -q 'Tier 2\|clean-PATH'` exits 0 (Verification block references Tier 2).
    - `awk '/^## v0\.2\.0/{p=1} /^## v0\.1\.28/{exit} p' CHANGELOG.md | grep -q 'Tier 3\|parity diff'` exits 0 (Verification block references Tier 3).
  </acceptance_criteria>

  <done>
    `01-VERIFICATION.md` records all five tiers with the verifier's recorded results from Task 3, all six locked decisions (D-01 through D-06) traced to their realizations, and a Sign-Off checklist. `CHANGELOG.md` has a v0.2.0 entry at the top mirroring the v0.1.28 structure (Added/Changed/Removed/Verification), referencing the same commands and outcomes as the verification artifact. The repo is structurally ready for the human releaser to run `git tag v0.2.0 && git push origin main --tags` per the v0.1.28 release pattern.
  </done>
</task>

</tasks>

<verification>
After all four tasks land, the Phase 1 gate (per `01-VALIDATION.md` "Phase gate"):

```bash
# Tier 1 — green from Task 2 + Task 4 sanity.
cargo fmt --manifest-path src-tauri/Cargo.toml --check
cargo check --manifest-path src-tauri/Cargo.toml
test $(cargo check --manifest-path src-tauri/Cargo.toml 2>&1 | grep -c '^warning:') -eq 0
cargo test --manifest-path src-tauri/Cargo.toml --lib
pnpm build
git diff --check

# Tier 2 + Tier 3 — recorded in 01-VERIFICATION.md from Task 3 checkpoint.
test -f .planning/phases/01-founder-sync-hardening/01-VERIFICATION.md
grep -q 'Tier 2.*PASS' .planning/phases/01-founder-sync-hardening/01-VERIFICATION.md
grep -q 'Tier 3.*\(zero diff\|documented\)' .planning/phases/01-founder-sync-hardening/01-VERIFICATION.md

# Versions bumped (Task 2).
grep -q '"version": "0.2.0"' package.json
grep -q '"version": "0.2.0"' src-tauri/tauri.conf.json
grep -q '^version = "0.2.0"' src-tauri/Cargo.toml
grep -q '## New In v0.2.0' README.md

# CHANGELOG entry (Task 4) — at top of file, above v0.1.28.
head -20 CHANGELOG.md | grep -q '^## v0.2.0'
grep -q '^## v0.1.28' CHANGELOG.md

# All 12 VALIDATION.md task IDs accounted for.
grep -c '01-frontmatter-parser\|01-status-normalize\|01-workspace-id-fallback\|01-request-body-dup-keys\|01-merge-artifacts\|01-kpi-employee-resolve\|01-onboarding-apply-guard\|01-report-struct-shape\|01-kpi-snapshot-sqlite\|01-fixture-vault-parity\|01-real-vault-diff\|01-clean-path-app-run' .planning/phases/01-founder-sync-hardening/01-VERIFICATION.md
# Expected: >= 12 (each task ID referenced at least once)
```
</verification>

<success_criteria>
- All five Verification Ladder tiers from `01-RESEARCH.md` §9 run to completion and recorded in `.planning/phases/01-founder-sync-hardening/01-VERIFICATION.md`.
- All 12 task IDs from `01-VALIDATION.md` Per-Task Verification Map are resolved (10 automated via `cargo test --lib` from Plan 01-02; 2 manual via Task 3 checkpoint).
- All four canonical version files at `0.2.0`; `Cargo.lock` regenerated; README's `## New In v0.2.0` section captures the user-visible changes.
- `CHANGELOG.md` v0.2.0 entry at top of file mirrors the v0.1.28 structure exactly (Added/Changed/Removed/Verification).
- Locked decisions traceability:
  - **D-01** (native Rust importer at `src-tauri/src/vault/parity.rs`): Plan 01-02 implements; this plan verifies via Tier 2.
  - **D-02** (dual-path, default rust, Node fallback for one release): Plan 01-01 wires; this plan keeps the bundled script in `tauri.conf.json` (Tier 4 confirms).
  - **D-03** (full parity, 4 note families): Plan 01-02 implements; this plan verifies via Tier 3 parity diff.
  - **D-04** (preserve JSON-on-disk report contract): Plan 01-02 regression-locks via test; this plan re-verifies via Tier 3 against real vault.
  - **D-05** (straight to stable): no beta channel; CHANGELOG and version bumps go straight to stable.
  - **D-06** (cross-AI peer review): captured in `01-REVIEWS.md` between plan-phase and execute-phase (the orchestrator handles invocation; this plan records the outcome in 01-VERIFICATION.md).
- Phase 1 ROADMAP success criteria 1-5 each have a recorded verification step (1 → Tier 2; 2 → Tier 1 + Tier 2; 3 → Tier 3; 4 → Tier 4; 5 → unchanged Worker contract regression-locked by `report_struct_serializes_to_node_compatible_json` from Plan 01-02).
- The repo is at a `git tag v0.2.0`-able state — the actual tag command is a human action recorded in `tasks/todo.md`'s Review block per the v0.1.28 release pattern; this plan does NOT create the tag.
- CONVENTIONS.md honored: single `commands/mod.rs` not split, `Result<T, String>` everywhere, `eprintln!("[vault-parity] …")` cadence, inline `#[cfg(test)] mod tests`, Cargo.lock committed, no new test directory.
</success_criteria>

<output>
After completion, create `.planning/phases/01-founder-sync-hardening/01-03-SUMMARY.md` with:
- Tier-by-Tier verification results (1: green; 2: PASS/FAIL with notes; 3: zero-diff or documented diffs; 4: bundle inspection result; 5: pre-tag green).
- The recorded `cargo test --lib` test count (target: v0.1.28 baseline + 10 = 53 total active + 3 ignored from baseline + 1 new ignored = 53 passed / 4 ignored).
- All 12 VALIDATION.md task IDs marked green with the date and verifier.
- Phase 1 ROADMAP success criteria 1-5 each mapped to a verification artifact (Tier number + line in 01-VERIFICATION.md).
- Hand-off note for the human releaser: "All four version files at 0.2.0. CHANGELOG entry written. 01-VERIFICATION.md complete. Ready for `git tag v0.2.0` and `git push origin main --tags` per the v0.1.28 release pattern. Phase 2 (#46 vault backfill) starts next; v0.2.1 will close the dual-path by removing `vault_sync_runtime` setting and the bundled Node script."
- Update STATE.md "Current Position" to mark Phase 1 plan 3/3 complete.
</output>
